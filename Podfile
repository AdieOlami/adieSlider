platform :ios, '9.0'

target 'adieSlider' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for adieSlider
  pod 'PureLayout'
  pod 'ChameleonFramework'

  target 'adieSliderTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'adieSliderUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
